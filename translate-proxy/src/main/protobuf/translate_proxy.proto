syntax = "proto3";

package com.customer.protos;

service Customer {
    rpc sendRequestRpc ( Request ) returns ( Reply ) {}
}

message Request {
    string customerId = 1;
    string customerPlan = 2;
}

message Reply {
    string message = 1;
}
